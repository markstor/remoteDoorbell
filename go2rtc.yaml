streams:
    stream: 
        # - ffmpeg:device?video=0#video=h264
        # - exec:ffplay -fflags nobuffer -f alaw -ar 8000 -i -#backchannel=1
        - ffmpeg:/home/marc/doorbell/testvid.mp4#video=copy#audio=pcmu
        - exec:ffmpeg -y -fflags nobuffer -f alaw -ar 8000 -i - -c:a libmp3lame -b:a 64k /home/marc/doorbell/output.mp3#backchannel=1